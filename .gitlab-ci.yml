image: maven:3.5.3-jdk-8
  
stages:
  - tests
  - deploiement
  
tests:
  stage: tests
  script: 
  - echo 'test'
  only:
  - branches
  
deploiement_dev:
  stage: deploiement
  script: 
    - mvn package -DskipTests
  artifacts:
    paths:
    - target/*.jar
    - src/main/resources/*.properties
    expire_in: 1h
  only:
    - dev

deploiement_master:
  stage: deploiement
  script: 
    - mvn package -DskipTests
  artifacts:
    paths:
    - target/*.jar
    - src/main/resources/*.properties
    expire_in: 1h
  only:
    - master